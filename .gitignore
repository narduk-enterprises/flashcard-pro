# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
.turbo

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files — use Doppler, not .env files
.env
.env.*

# Cloudflare
.wrangler/

# Build artifacts
*.bak

# Test artifacts
playwright-report/
test-results/

# Prod cleanup/seed (generated by tools/cleanup-e2e-and-seed-prod.ts)
apps/web/drizzle/cleanup-e2e-prod.sql
apps/web/drizzle/seed-demo-prod.sql

# ESLint plugin build artifacts (rebuilt via `pnpm run build:plugins`)
packages/eslint-config/*/dist/
# ESLint caches
.eslint-results.json

# Secrets & credential artifacts (use Doppler, never commit these)
*-secrets.json
*.jsonl
doppler.json
doppler.yaml
lint-results.txt
